name: Workflow Test

on:
  workflow_dispatch:

jobs:
  lint-main-ui:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'release/')
    steps:
      - name: test
        run: |
          echo "test"
